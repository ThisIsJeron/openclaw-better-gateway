{
  "id": "better-gateway",
  "name": "Better Gateway",
  "version": "1.0.0",
  "description": "Enhanced gateway UI with auto-refresh and WebSocket reconnection",
  "main": "dist/index.js",
  "configSchema": {
    "type": "object",
    "properties": {
      "upstreamHost": {
        "type": "string",
        "default": "localhost",
        "description": "Hostname of the upstream gateway UI"
      },
      "upstreamPort": {
        "type": "number",
        "default": 18789,
        "description": "Port of the upstream gateway UI"
      },
      "reconnectIntervalMs": {
        "type": "number",
        "default": 3000,
        "description": "WebSocket reconnection interval in milliseconds"
      },
      "maxReconnectAttempts": {
        "type": "number",
        "default": 10,
        "description": "Maximum number of reconnection attempts before giving up"
      }
    },
    "required": []
  }
}
